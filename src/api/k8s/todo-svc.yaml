apiVersion: v1
kind: Service
metadata:
  name: todo-app
  namespace: etudiant28-ns
  labels:
    app: todo-app
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30374
  selector:
    app: todo-app
  type: NodePort